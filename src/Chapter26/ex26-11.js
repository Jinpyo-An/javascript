const multiply = (x, y) => x * y;
multiply(2, 3);

const power = x => x ** 2;
power(2);

const power2 = x => {
    return x ** 2;
};

// const arrow = () => {return const x = 1;};

const arrow = () => {
    const x = 1;
};